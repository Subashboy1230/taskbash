# Morning QA — 2026-05-30

Run by the 8 AM scheduled task. Subash, please skim before pushing.

## TL;DR

The 2 AM overnight task crashed mid-write (the `.taskbash-overnight-status.md` it was supposed to leave never landed and `.git/index.lock` was orphaned in the repo). The actual code work it did finished on disk but never got committed. I picked that work up, verified 7 of 8 fixes against your spec, made one small consistency fix, and committed everything as `2b7fda7`. I did NOT push — see Build Status below.

## Build Status

- `npx tsc --noEmit`: **PASS** (0 errors, both before and after my edit)
- `npm run build`: **NOT VERIFIED**. The sandbox shell caps any single command at 45 s and the production Next build needs >60 s from cold. Background processes also die with the parent shell (PID-namespaced bwrap), so I can't keep a build alive across calls. I left the commit local. Please run `npm run build` locally, then `git push origin main` if it's clean.

## What the overnight task actually got done (before crashing)

All 16 files were modified on disk but never staged/committed. Recovered into commit `2b7fda7`. Diff summary:

```
 app/_components/app-header.tsx       |   4 +-
 app/_components/page-shell.tsx       | NEW (3-col PageShell wrapper)
 app/activity/page.tsx                |  46 +/-
 app/connections/connections-view.tsx |  60 +/-
 app/connections/page.tsx             |  26 +/-
 app/globals.css                      |  19 +/-  (tag chip palette)
 app/handled/page.tsx                 |  27 +/-
 app/login/page.tsx                   |   4 +-
 app/network/page.tsx                 |  46 +/-
 app/observability/page.tsx           |  31 +/-
 app/profile/page.tsx                 |  44 +/-
 app/settings/functions/page.tsx      |  29 +/-
 app/today/today-calendar-column.tsx  | 192 +/-  (collapsible + click any day)
 app/today/today-view.tsx             |  13 +/-  (Re-run tasks button)
 lib/function-color.ts                |  19 +/-  (brightened dark palette)
 scripts/auto-approve-cowork.sh       | NEW
```

## 8-issue checklist

| # | Issue | Status | Notes |
|---|---|---|---|
| 1 | Calendar day-cell selection bug | DONE | `text-canvas` on `bg-accent` for today, `ring-2 ring-ink text-ink` for selected. No `text-white` left in the column. |
| 2 | Re-run tasks button | DONE | `Button variant="outline" size="sm"` with `<RefreshCw/>` icon + "Re-run tasks" label. `aria-label`, `title`, both correct. |
| 3 | Calendar collapsible | DONE | `ChevronsRight` to collapse, `ChevronsLeft` to expand. `localStorage` key `taskbash:calendarCollapsed`. Default expanded. |
| 4 | Click-day-opens-events | DONE | All in-month days clickable (not just days with items). `SelectedDaySection` renders below the grid showing tasks due on that ISO date. |
| 5 | Sidebar + calendar on every page | DONE | `PageShell` used in: connections, handled, observability, settings/functions, activity, network, profile, login. Only `today/today-view.tsx` still imports `AppHeader`, and it's gated behind the `hideHeader` prop. |
| 6 | Color / typography pass | DONE | Tag chip backgrounds bumped 12% to 18%, foregrounds lifted to 200-shade hues. Function-color fallback palette swapped to bright dark-mode hues. Heading bumped from 28px to 30px. |
| 7 | Card wrap | PARTIAL | Today's events list IS wrapped in `<Card>` (today-calendar-column.tsx). FilterBar uses Card-like classes (`rounded-xl border border-line bg-surface/40 p-4 shadow-sm`) but not the literal `<Card>` component. Visual outcome matches spec; literal compliance does not. Tiny cleanup if you want strict parity. |
| 8 | Sheet for detail panel | SKIPPED | Per your spec, optional. Detail panel still renders inline via TodayShell. |

## What I fixed this pass

- `app/today/today-view.tsx`: renamed `"Refresh failed"` -> `"Re-run failed"` in the error banner (both the user-visible string and the default error fallback). The overnight task swapped the button label but missed the matching error state.

## Static grep results (clean)

- `grep "Refresh" app/today/today-view.tsx`: only internal symbols (`RefreshCw` icon, `handleRefresh` fn, `refreshError` state, `requestRefresh` action). Zero user-visible strings.
- `grep -rn "ToDoo" app/`: 0 hits.
- `grep "AppHeader" app/`: 2 hits in `today-view.tsx` (both gated by `hideHeader`), 1 in `app-header.tsx` itself.
- `grep "text-white" app/today/today-calendar-column.tsx`: 0 hits.

## Outstanding / for Subash's eyes

1. **Run `npm run build` locally.** I can't verify it from the sandbox. If green, `git push origin main` will send commit `2b7fda7`.
2. **Card wrap on FilterBar.** Decide whether to literal-`<Card>` it or leave the Card-like classes. Cosmetic-only.
3. **Stale .git lock files.** The 2 AM crash left `.git/index.lock`, `.git/HEAD.lock`, `.git/objects/maintenance.lock`, and a pile of `.git/objects/*/tmp_obj_*` files. The sandbox can't `rm` them (fuse mount permissions) but git itself worked around them. Worth a `git gc` from your terminal to clean them up — none of them are blocking, but they'll keep producing "unable to unlink" warnings on every git op.

## Top 3 next priorities (my read)

1. **Network page MVP.** `app/network/page.tsx` was migrated to PageShell but the page body is still the legacy shell — no actual Network UX yet.
2. **Sheet migration for detail panel.** Issue 8 from this morning, still outstanding. shadcn Sheet would clean up the DetailPanel/TodayShell two-branch render in today-shell.tsx.
3. **FilterBar literal `<Card>` swap.** Tiny consistency win, ~5 min.

## Commit pushed

- Local: `2b7fda7 Morning QA: verify 7 of 8 overnight fixes + rename Refresh failed string`
- Pushed: **NONE** (held on local; awaiting your `npm run build` green light)
